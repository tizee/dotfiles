# Type Checking

## Python

Use `ty` - a fast type checker from Astral (makers of Ruff).

```bash
# Quick check (no install needed)
uvx ty check

# Watch mode
ty check --watch
```

For detailed usage, invoke the `python-ty` skill.
