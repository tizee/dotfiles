interface:
  display_name: "Uv Script Tool"
  short_description: "Create standalone executable Python scripts with uv"
  default_prompt: "Write a uv script that downloads a file from a URL with a progress bar"
