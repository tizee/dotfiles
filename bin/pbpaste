#!/usr/bin/env zsh

case $(uname -s) in
  Darwin*)
     pbpaste $@
    ;;
  Linux*)
     # xclip is avaliable in most Linux distributions
     # TODO test in Linux
     xclip -selection clipboard -o
    ;;
  *)
    exit 1
    ;;
esac
